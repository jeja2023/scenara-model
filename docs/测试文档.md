# 测试文档

```powershell
.\.venv\Scripts\python.exe scripts\check_versions.py
.\.venv\Scripts\python.exe -m ruff check src scripts tests migrations
.\.venv\Scripts\python.exe -m pytest
```

当前测试覆盖本地训练/导出/评估适配器、模型包、元数据存储、鉴权和 API，并新增 FastReID manifest 物化、环境锁/CMC+mAP 门禁、部署反馈签名/幂等/乱序回放、PostgreSQL 备份恢复安全开关和 S3 provider 读写协议。正式资格验证仍需真实 FastReID 固定评估集、模型权属、目标 PostgreSQL/S3 和 Model Package 到 Core 的端到端证据。

`1.0.0-dev.4` 的基线为 116 项 Python 测试。当前工作区锁定 Contracts `1.2.0`，覆盖人像 ReID 对 `scenara.portrait.surveillance-review.v1` 的数据引用和 bundle 校验；另有 Windows FastReID GPU 冒烟、隔离 PostgreSQL 备份恢复及 MinIO 写读演练记录，详见 [资格验证报告](资格验证报告_2026-09-04.md)。
