# 资格验证报告（2026-09-04）

适用版本：`1.0.0-dev.4`。

本报告记录在开发工作站执行的可复现实机验证。它不包含真实业务训练或发布资格，且不改变仓库的 `seed` 成熟度。

| 项目 | 结果 | 证据 / 边界 |
| --- | --- | --- |
| GPU 主机 | 通过 | NVIDIA GeForce RTX 3070 Laptop GPU，8,192 MiB 显存，驱动 596.08；Windows 非生产冒烟已通过 FastReID 模型构建和 CUDA embedding 前向。 |
| FastReID 运行时 | Windows 冒烟通过；Linux 容器待实测 | Windows：Python 3.12、PyTorch 2.5.1+cu121、FastReID 固定提交，`cuda:0` 输出 `[1, 512]`。生产：固定 Linux CUDA 11.6 / PyTorch 1.13.1 容器已加入。Docker Desktop 的 WSL `docker-desktop` 发行版当日为 stopped，Linux 引擎不可用，故未伪造容器通过记录。 |
| PostgreSQL 备份恢复 | 通过 | 在隔离 PostgreSQL 18.4 集群执行应用写读、custom-format `pg_dump`、恢复到独立数据库、恢复后读回校验。备份 SHA-256：`af91213b5f95f272e6c3e890d900561a301a27fc47b1fd97621e0e627d35b863`。 |
| PostgreSQL + MinIO 探针 | 通过 | 隔离 PostgreSQL 写读成功；MinIO bucket 写入、即时回读与 SHA-256 比对通过。报告和探针对象保存在本机 `artifacts/`，不纳入 Git。 |
| 数据、评估与权属 | 待外部输入 | 仓库不存在真实 ReID 图片、固定业务评估集或权属/审批原件。FastReID 物化器和生产门禁会拒绝缺失这些材料的运行。 |

资格证据目录：

```text
artifacts/postgres-qualification-20260904/source.dump
artifacts/minio-qualification-20260904/evidence/target-environment-qualification.json
```

两项本地临时服务已停止；上述目录仅保留隔离演练证据。执行真实训练前，先恢复 Docker Desktop 的 Linux 引擎，再按 [FastReID训练运行手册](FastReID训练运行手册.md) 构建并运行容器预检。
