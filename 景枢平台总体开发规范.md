# 景枢平台总体开发规范

**英文名称：Scenara Platform Development Specification**

**文档版本：** 1.2.1
**文档状态：** 正式开发规范
**适用范围：** 景枢全部核心项目
**规范所有者：** Scenara Platform Architecture
**最近修订：** 2026-08-15

## 修订记录

| 版本 | 日期 | 主要变更 |
| --- | --- | --- |
| 1.0.0 | 2026-08-15 | 建立四仓平台、ReID 闭环和统一基础规范 |
| 1.1.0 | 2026-08-15 | 明确 Core 共享控制面职责，采用 Vue 3，细化状态、ID、错误、事件、对象引用、数据拆分、测试和发布门禁 |
| 1.2.0 | 2026-08-15 | 增加统一前端设计系统、平台主题、视觉令牌、响应式、无障碍和界面验收门禁 |
| 1.2.1 | 2026-08-15 | 固化景枢 Logo 几何母体、中心对齐、曲面带间距和品牌资产同步规则 |

------

# 1. 项目总则

景枢（Scenara）是一套面向视觉人工智能的数据、模型、分析、特征和检索平台。

景枢必须形成完整的：

```text
数据
 ↓
数据集
 ↓
模型训练
 ↓
模型评估
 ↓
模型制品
 ↓
模型部署
 ↓
视觉分析
 ↓
特征生成
 ↓
智能检索
 ↓
结果反馈
 ↓
数据沉淀
 ↓
数据集迭代
 ↓
模型迭代
```

所有系统设计、代码实现、数据结构和接口设计必须围绕这一完整闭环进行。

## 1.1 规范用语

本文使用以下约束级别：

| 用语 | 含义 |
| --- | --- |
| 必须 / MUST | 发布、合并和跨仓库集成前必须满足，不满足即验收失败 |
| 禁止 / MUST NOT | 不得实现；发现后必须阻断发布并制定迁移方案 |
| 应当 / SHOULD | 默认执行；确需例外时必须记录 ADR、影响、责任人和退出条件 |
| 可以 / MAY | 可选能力，由目标项目根据阶段、成本和风险决定 |

规范冲突时按以下优先级处理：

```text
正式发布的 scenara-contracts
    ↓
本总体开发规范
    ↓
项目级 ADR、开发规范和产品标准
    ↓
现有实现
```

发现下级文档或实现与上级规范不一致时，不得静默兼容。责任项目必须创建整改项，并明确采用以下一种处理方式：

```text
修改实现
修改下级文档
通过正式评审修订上级规范
```

任何例外必须包含：适用范围、原因、风险、负责人、开始时间、复审时间和移除条件。永久例外必须进入 ADR。

------

# 2. 项目组成

景枢统一由四个 Repository 构成：

```text
jeja2023/
│
├── scenara
├── scenara-data
├── scenara-model
└── scenara-contracts
```

对应关系：

| Repository          | 中文名称     | 英文名称          | 项目标识            |
| ------------------- | ------------ | ----------------- | ------------------- |
| `scenara`           | 景枢核心平台 | Scenara Core      | `scenara`           |
| `scenara-data`      | 景枢数据平台 | Scenara Data      | `scenara-data`      |
| `scenara-model`     | 景枢模型平台 | Scenara Model     | `scenara-model`     |
| `scenara-contracts` | 景枢平台契约 | Scenara Contracts | `scenara-contracts` |

原：

```text
vision-model-lab
```

统一更名为：

```text
scenara-model
```

后续正式代码、文档、镜像、配置、接口均使用 `scenara-model`。

------

# 3. 总体产品架构

景枢总体架构分为：

```text
                         景枢 Scenara
                              │
                    景枢平台契约 Contracts
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
          ▼                   ▼                   ▼
    景枢核心平台          景枢数据平台          景枢模型平台
   Scenara Core        Scenara Data        Scenara Model
          │                   │                   │
          │                   │                   │
       生产运行             数据管理             模型研发
       视觉分析             数据集               模型训练
       特征计算             数据标注             模型评估
       智能检索             数据质量             模型管理
       结果管理             数据血缘             模型制品
          │                   │                   │
          └───────────────────┼───────────────────┘
                              │
                         基础设施层
                              │
              ┌───────────────┼───────────────┐
              │               │               │
              ▼               ▼               ▼
         PostgreSQL          Redis       S3 Provider
                                          (MinIO baseline)
              │
           pgvector
```

------

# 4. 产品战略

## 4.1 总体战略

景枢长期建设以下能力：

```text
数据 Data
模型 Model
运行 Runtime
特征 Feature
检索 Search
分析 Analysis
智能 Intelligence
```

最终形成：

```text
数据平台
    ↓
模型平台
    ↓
核心平台
    ↓
特征平台
    ↓
检索平台
    ↓
智能分析
```

------

# 5. 产品演进阶段

## 第一阶段：视觉基础平台

重点：

```text
人物检测
人物重识别
人脸检测
人脸特征
数据集管理
模型训练
模型评估
模型管理
模型部署
特征存储
人物检索
```

第一阶段必须完成：

> **人物重识别从数据集到训练、模型发布、生产推理、特征生成、图片检索的完整闭环。**

------

## 第二阶段：视觉能力扩展

增加：

```text
人体姿态
人体属性
步态识别
行为识别
目标检测
目标跟踪
场景分析
OCR
```

------

## 第三阶段：视觉检索平台

建设：

```text
图片检索
人物检索
人脸检索
视频检索
轨迹检索
文本检索
语义检索
时空检索
混合检索
```

------

## 第四阶段：多模态视觉智能

增加：

```text
图像特征
视频特征
文本特征
多模态特征
视觉语义理解
人物关系
事件关系
场景关系
```

支持：

```text
图搜图
图搜视频
文搜图
文搜视频
人物搜索
事件搜索
```

------

## 第五阶段：智能化平台

逐步建设：

```text
自动数据分析
自动难例发现
自动数据集构建
自动模型评估
自动模型选择
自动训练
智能检索
智能分析
智能流程
```

------

# 6. 四个项目职责

------

# 6.1 景枢核心平台

Repository：

```text
scenara
```

英文：

```text
Scenara Core
```

能力：

```text
媒体管理 Media Management
运行管理 Run Management
流程管理 Pipeline Management
算子管理 Operator Management

视觉分析 Vision Analysis

人物检测 Person Detection
人物重识别 Person Re-Identification
人脸检测 Face Detection
人脸识别 Face Recognition
人体姿态 Pose Estimation
人体属性 Person Attribute

特征管理 Feature Management
特征库 Feature Store
向量检索 Vector Search
人物检索 Person Search
轨迹检索 Track Search

结果管理 Result Management
反馈管理 Feedback Management
难例确认与导出 Hard Sample Qualification and Export

模型运行 Model Runtime
模型准入 Model Admission
模型部署 Model Deployment
模型回滚 Model Rollback
```

核心平台负责：

> **共享控制面、生产运行和视觉智能能力。**

核心平台同时统一提供：

```text
Scenara Console
Scenara API Gateway
Scenara SDK
Organization / Project / IAM
Permission / Audit
Product Catalog
跨仓库契约目录和治理入口
```

数据平台和模型平台不得重复建设独立的 Console、用户体系、API 网关、SDK 发布体系或审计查询中心。专业平台可以实现自己的领域 API 和后台服务，由核心平台统一认证、授权、代理或聚合。

核心平台不负责：

```text
模型训练任务
实验跟踪
训练算力调度
完整数据集治理
标注生产
数据集质量与血缘事实来源
```

------

# 6.2 景枢数据平台

Repository：

```text
scenara-data
```

英文：

```text
Scenara Data
```

能力：

```text
数据资产 Data Asset
媒体数据 Media Data
样本 Sample

数据集 Dataset
数据集版本 Dataset Version

标注 Annotation

人物样本 Person Sample
轨迹样本 Track Sample

数据清洗 Data Cleaning
数据质量 Data Quality
数据血缘 Data Lineage

难例数据 Hard Sample Data

数据集构建 Dataset Builder
数据集导入 Dataset Import
数据集导出 Dataset Export
```

数据平台负责：

> **数据资产和数据集生命周期管理。**

------

# 6.3 景枢模型平台

Repository：

```text
scenara-model
```

英文：

```text
Scenara Model
```

能力：

```text
实验管理 Experiment Management

模型训练 Model Training
训练任务 Training Job

模型评估 Model Evaluation
模型基准测试 Model Benchmark

模型 Model
模型版本 Model Version
模型检查点 Model Checkpoint
模型制品 Model Package

模型注册 Model Registry
模型发布 Model Release
模型制品发布 Model Package Publication
模型版本撤回 Model Version Withdrawal
```

模型平台负责：

> **视觉模型研发、训练、评估、注册和模型制品管理。**

模型平台的“发布”是向核心平台交付不可变模型制品和证据，不等同于生产部署。生产模型激活、流量切换和回滚由核心平台负责。

------

# 6.4 景枢平台契约

Repository：

```text
scenara-contracts
```

英文：

```text
Scenara Contracts
```

负责统一：

```text
能力词典 Capability Dictionary
实体词典 Entity Dictionary
数据结构 Data Schema
接口结构 API Schema
事件结构 Event Schema
错误码 Error Code
状态 State
对象引用 Object Reference
版本规范 Versioning
兼容性 Compatibility
```

该项目是景枢：

> **公共语言、数据契约和系统边界的唯一来源。**

其他仓库可以保存已发布契约包、生成代码或只读镜像，但不得把镜像作为新的事实来源。镜像必须记录来源版本和摘要，并通过 CI 验证零漂移。

------

# 7. 项目依赖关系

允许：

```text
scenara
    ↓
scenara-contracts

scenara-data
    ↓
scenara-contracts

scenara-model
    ↓
scenara-contracts
```

禁止：

```text
scenara → scenara-data 源码

scenara → scenara-model 源码

scenara-data → scenara 源码

scenara-data → scenara-model 源码

scenara-model → scenara 源码

scenara-model → scenara-data 源码
```

项目之间必须通过：

```text
API
Event
Object Reference
Contract
```

进行通信。

------

# 8. 景枢能力命名规范

所有核心能力必须同时定义：

```text
中文名称
英文名称
简称
能力 ID
能力层级
所属责任仓库
```

能力分为两层：

| 层级 | 用途 | 示例 | 是否允许跨仓库使用 |
| --- | --- | --- | --- |
| 平台能力 | 产品、权限、API、事件和跨仓库契约 | `person_reid` | 允许，必须进入能力词典 |
| 实现能力 | 算子、模型子能力和内部流水线 | `body_embedding` | 默认禁止，仅限责任仓库内部 |

平台能力不得直接暴露训练框架、推理引擎或具体模型名称。内部实现能力不得代替平台能力出现在跨仓库契约中。

例如：

```text
中文名称：人物重识别
英文名称：Person Re-Identification
简称：ReID
能力 ID：person_reid
```

例如：

```text
中文名称：特征库
英文名称：Feature Store
能力 ID：feature_store
```

例如：

```text
中文名称：人物检索
英文名称：Person Search
能力 ID：person_search
```

------

# 9. 能力 ID 规范

平台能力 ID 必须：

```text
小写
下划线分隔
稳定
唯一
```

示例：

```text
person_detection
person_reid
face_detection
face_recognition
pose_estimation
feature_store
vector_search
person_search
model_training
model_evaluation
model_registry
dataset
dataset_version
```

能力 ID 一经发布不得随意修改。

现有实现中与平台能力不一致的内部名称必须采用显式映射。例如：

```yaml
platform_capability_id: person_reid
implementation_capability_ids:
  - body_reid
  - body_embedding
```

不得通过无版本别名静默改变已发布能力含义。能力重命名必须保留兼容期，并在契约变更记录中声明旧 ID、替代 ID 和停止支持版本。

------

# 10. 景枢能力词典

`scenara-contracts` 必须建立：

```text
docs/
└── 景枢能力词典.md
```

每项能力必须记录：

```text
中文名称
英文名称
简称
能力 ID
所属平台
功能说明
输入
输出
状态
版本
责任仓库
内部实现映射
弃用信息
```

示例：

```yaml
id: person_reid
name_zh: 人物重识别
name_en: Person Re-Identification
abbreviation: ReID
platform: scenara-model, scenara
status: active
```

------

# 11. 核心实体规范

统一使用：

```text
媒体 Media
样本 Sample
数据集 Dataset
数据集版本 Dataset Version
标注 Annotation

人物 Person
轨迹 Track
人物轨迹 Person Track

特征 Feature
特征空间 Feature Space

模型 Model
模型版本 Model Version
模型制品 Model Package

实验 Experiment
训练任务 Training Job

推理运行 Inference Run
分析结果 Analysis Result
```

------

# 12. 数据集规范

数据集结构：

```text
Dataset
    │
    ├── Dataset Version
    ├── Dataset Version
    └── Dataset Version
```

数据集版本必须不可变。

禁止：

```text
修改已经发布的数据集版本
```

修改数据必须创建新的版本。

------

# 13. 数据集与模型关系

模型训练只能引用：

```text
Dataset Version
```

禁止模型平台维护自己的训练数据事实来源。

标准关系：

```text
Scenara Data
      │
      ▼
Dataset
      │
      ▼
Dataset Version
      │
      ▼
Scenara Model
```

------

# 14. 模型规范

模型结构：

```text
Model
    │
    ├── Model Version
    ├── Model Version
    └── Model Version
```

模型版本必须不可覆盖。

每个模型版本必须绑定：

```text
训练数据集版本
训练配置
代码版本
运行环境
评估结果
模型文件
```

------

# 15. 模型制品规范

模型平台向核心平台交付：

```text
Model Package
```

模型制品至少包含：

```text
模型文件
模型名称
模型版本
模型类型
输入定义
输出定义
运行环境
数据集版本
评估结果
校验值
```

推荐结构：

```text
model-package/
├── model
├── model-card.yaml
├── input-schema.yaml
├── output-schema.yaml
├── metadata.yaml
├── labels.yaml
└── checksums.txt
```

------

# 16. 人物重识别规范

第一阶段人物重识别统一：

```text
人物检测
    ↓
人物裁剪
    ↓
人物重识别
    ↓
人物特征
    ↓
特征库
    ↓
人物检索
```

人物重识别能力：

```text
中文：人物重识别
英文：Person Re-Identification
简称：ReID
ID：person_reid
```

------

# 17. FastReID 规范

人物重识别训练使用：

```text
FastReID
```

但 FastReID 不得直接成为业务层。

统一通过：

```text
Scenara Model
      ↓
ReID Adapter
      ↓
FastReID
```

FastReID 负责：

```text
网络
损失函数
采样
优化器
学习率
训练
评估
检查点
```

景枢模型平台负责：

```text
数据集引用
训练任务
实验
训练记录
评估记录
模型制品
模型注册
模型发布
```

------

# 18. 人物重识别数据规范

至少支持：

```text
image
person_id
camera_id
bbox
dataset_split
```

数据集至少支持：

```text
train
query
gallery
```

训练数据必须通过数据平台进行版本化管理。

------

# 19. 模型评估规范

人物重识别必须支持：

```text
Rank-1
Rank-5
Rank-10
mAP
```

所有评估结果必须绑定：

```text
Model Version
Dataset Version
Evaluation
```

------

# 20. 特征规范

生产特征必须绑定：

```text
Model
Model Version
Feature Dimension
Distance Metric
Feature Space
Source
Created Time
```

示例：

```text
Feature Space:
person/body/reid

Model:
person-reid

Version:
1.0.0

Dimension:
512

Metric:
Cosine
```

不同模型版本的特征不得默认混用。

------

# 21. 人物规范

人物：

```text
Person
```

表示统一人物实体。

人物可以关联：

```text
Person Track
Feature
Media
```

人物不得等同于：

```text
Detection
Track
Feature
Sample
```

------

# 22. 轨迹规范

轨迹：

```text
Track
```

表示单个媒体中的连续人物目标。

轨迹必须关联：

```text
Media
Camera
Start Time
End Time
Detection
Feature
```

轨迹可以关联：

```text
Person
```

------

# 23. 特征库规范

第一阶段：

```text
PostgreSQL
+
pgvector
```

统一抽象：

```text
Feature Store
```

业务层只调用：

```text
FeatureStore
```

禁止业务代码直接依赖 pgvector。

------

# 24. 人物检索规范

第一阶段必须实现：

```text
Image
 ↓
Person Search
 ↓
Feature Search
 ↓
Person / Track
 ↓
Media
 ↓
Timestamp
```

返回至少：

```text
Person
Track
Media
Timestamp
Similarity
Model Version
```

------

# 25. 核心平台生产模型流程

模型进入生产必须经过：

```text
Model Package
      ↓
Model Admission
      ↓
Validation
      ↓
Approval
      ↓
Activation
      ↓
Production Runtime
```

未经准入的模型不得进入生产环境。

------

# 26. 数据闭环

核心平台产生的数据：

```text
Inference Result
Analysis Result
Feedback
Hard Sample
```

可以进入数据平台：

```text
Core
 ↓
Result
 ↓
Feedback
 ↓
Hard Sample
 ↓
Data
 ↓
Dataset Version
```

------

# 27. 完整模型闭环

```text
Dataset
 ↓
Dataset Version
 ↓
Training
 ↓
Evaluation
 ↓
Model Version
 ↓
Model Package
 ↓
Model Registry
 ↓
Model Admission
 ↓
Deployment
 ↓
Inference
 ↓
Feature
 ↓
Search
 ↓
Feedback
 ↓
Hard Sample
 ↓
Dataset
```

------

# 28. 基础设施统一规范

三个平台统一使用：

```text
PostgreSQL
Redis
经认证的 S3-compatible Object Storage Provider
```

第一阶段私有化部署基线为 MinIO，但平台边界必须保持 Provider 中立。其他 S3-compatible Provider 通过兼容性、完整性、安全、生命周期和恢复测试后可以使用。

统一原则：

> **技术统一、资源统一、访问规范统一、业务数据隔离。**

------

# 29. PostgreSQL

统一：

```text
PostgreSQL
```

建议逻辑隔离：

```text
scenara_core
scenara_data
scenara_model
```

禁止：

```text
跨系统直接读取业务表
跨系统直接修改业务表
```

------

# 30. Redis

统一：

```text
Redis
```

用于：

```text
Cache
Queue
Task State
Distributed Lock
Temporary State
```

禁止将 Redis 作为核心业务事实数据库。

------

# 31. 对象存储

统一抽象：

```text
Object Storage Provider
```

第一阶段基线实现：

```text
MinIO
```

建议：

```text
scenara-media
scenara-datasets
scenara-models
scenara-results
scenara-artifacts
scenara-backups
```

跨系统文件必须通过对象引用访问。

------

# 32. 对象引用规范

本节强制适用于跨仓库传递的文件、模型、数据集、样本、结果和制品。仓库内部可以使用受控的 `object_key` 或本地开发路径，但不得把内部路径写入跨仓库契约、事件或持久化引用。

跨仓库禁止：

```text
/mnt/data/xxx
/data/models/xxx
C:\models\xxx
```

统一：

```yaml
bucket: scenara-models
key: person-reid/1.0.0/model.onnx
version: "3Lg..."
checksum: sha256:0123456789abcdef
size_bytes: 104857600
content_type: application/octet-stream
```

对象引用必须满足：

```text
bucket 和 key 非空
checksum 使用明确算法前缀
已发布对象不可覆盖
读取时重新校验 checksum
权限按 Organization / Project / Service Account 检查
跨仓库访问使用短期授权、预签名地址或服务间凭据
```

对象存储 Provider、桶名和访问地址不得成为业务层判断条件。

------

# 33. API 规范

所有跨平台 API 必须定义在：

```text
scenara-contracts
```

统一：

```text
/api/v1/
```

API 必须定义：

```text
Request
Response
Error
Authentication
Authorization
Idempotency
Version
```

可变更资源的写接口必须说明是否支持幂等。创建任务、接收事件、导入数据、准入模型和发布版本等可重试操作必须接受 `Idempotency-Key` 或业务唯一键。

跨仓库 API 必须提供：

```text
OpenAPI 或等价机器可读定义
请求与响应示例
认证方式
权限 ID
错误码集合
分页、过滤和排序语义
超时与重试语义
兼容和弃用策略
```

核心平台作为统一 API 网关时，可以保留稳定公共路径并代理专业平台。代理不得改变契约语义、租户和项目边界、请求 ID、追踪上下文或错误码。

------

# 34. 事件规范

第一阶段事件目录至少包括：

```text
dataset.created
dataset.version.created

training.started
training.completed
training.failed

model.registered
model.package.created
model.released
model.activated
model.rollback
model.deployment.changed

inference.completed
hard_sample.created
result.available
result.delta
```

事件必须定义在：

```text
scenara-contracts
```

事件名称使用小写点分形式，格式为：

```text
<domain>.<entity-or-process>.<action>
```

每个事件必须包含统一信封：

```json
{
  "event_id": "evt_...",
  "event_type": "model.deployment.changed",
  "event_version": "1.0",
  "occurred_at": "2026-08-15T10:30:00Z",
  "producer": "scenara",
  "tenant_id": "...",
  "project_id": "...",
  "request_id": "...",
  "trace_id": "...",
  "data": {}
}
```

生产方必须使用 Outbox 或等价机制保证业务提交与事件登记的一致性。消费方必须按 `event_id` 幂等处理。事件投递允许至少一次，不得假设恰好一次。

已发布事件不得原地修改含义。兼容字段追加使用次版本，不兼容变更使用新的主版本或事件类型。

------

# 35. 错误码规范

统一错误格式：

```json
{
  "schema_version": "1.0",
  "request_id": "xxx",
  "error": {
    "code": "MODEL_NOT_FOUND",
    "message": "模型不存在",
    "details": {}
  }
}
```

错误码统一维护在 `scenara-contracts`。错误消息可以本地化，但错误码含义不得随语言或项目变化。

禁止不同项目定义含义相同但名称不同的错误码。

禁止把数据库异常、文件路径、密钥、令牌或内部堆栈直接返回给客户端。未知错误统一映射为登记过的服务端错误码，并保留 `request_id` 用于审计和排障。

------

# 36. ID 规范

核心业务 ID 必须是全局唯一、稳定且不依赖数据库自增序列的不透明字符串。允许采用：

```text
UUID
UUIDv7
<entity_prefix>_<UUID-or-random-id>
```

跨系统实体不得使用数据库自增 ID 作为唯一业务 ID。

新建高写入量、需要按时间排序的实体应优先使用 UUIDv7。既有 `run_`、`dst_`、`dsv_` 等前缀 ID 保持兼容，不得仅为格式统一批量改写。

客户端不得解析 ID 内部结构或依据前缀推断权限、类型和路由。

------

# 37. 时间规范

数据库统一：

```text
UTC
```

API 使用：

```text
ISO 8601 / RFC 3339
UTC
必须包含时区
```

例如：

```text
2026-08-15T10:30:00Z
```

公共 API 和跨仓库事件不得新增 Unix 浮点秒时间字段。既有浮点时间字段必须通过版本化迁移转换为带时区字符串；兼容期内如同时提供新旧字段，必须声明优先级和移除版本。

持续时间、延迟和媒体时间轴使用明确单位字段，例如 `duration_ms`、`latency_ms`、`pts_ms`，不得与绝对时间混用。

------

# 38. 状态规范

状态由责任实体定义状态机，不要求所有实体共用同一集合。代码枚举名可以大写，API 和事件中的线值统一使用小写。

任务状态至少支持：

```text
queued
running
succeeded / completed
failed
cancelled
```

需要暂停、恢复或异步取消的任务可以增加：

```text
pausing
paused
cancelling
```

模型发布状态至少支持：

```text
candidate / draft
validating
validated
approved
active
retired
```

数据集版本状态至少支持：

```text
draft
building
ready / validated
published
archived / retired
```

每个实体必须在契约中定义：允许状态、合法转换、终态、可重试状态、转换权限和审计动作。责任仓库可以采用更完整的状态机，但跨仓库输出必须使用已登记状态或显式映射，禁止创建无定义的同义状态。

------

# 39. 身份权限规范

统一使用景枢核心平台身份体系：

```text
Organization
Project
User
Role
Service Account
Permission
```

数据平台和模型平台不得创建第二套用户体系。

------

# 40. 日志规范

所有服务必须记录：

```text
timestamp
level
service
module
request_id
trace_id
message
```

可以同时记录 W3C `traceparent`、`span_id`、`tenant_id`、`project_id`、`principal_id` 和结构化业务字段。`trace_id` 可以从合法 `traceparent` 中提取，但不得只记录无法直接检索的原始头。

日志时间使用 UTC RFC 3339。日志不得包含密码、访问令牌、API Key、私钥、完整人脸特征或未脱敏个人数据。

跨项目请求必须传递：

```text
request_id
trace_id
```

------

# 41. 审计规范

以下操作必须记录：

```text
Dataset Create
Dataset Publish
Dataset Delete

Training Start
Training Cancel

Model Register
Model Approve
Model Release
Model Activate
Model Rollback
Model Retire

Feature Delete
Permission Change
```

------

# 42. 技术栈规范

第一阶段统一：

| 类型       | 技术               |
| ---------- | ------------------ |
| 后端       | Python             |
| 后端框架   | FastAPI            |
| 统一 Console | Vue 3 + TypeScript |
| 前端路由   | Vue Router         |
| 前端图标   | Lucide Vue         |
| 数据库     | PostgreSQL         |
| 向量       | pgvector           |
| 缓存       | Redis              |
| 对象存储   | S3-compatible Provider（MinIO 基线） |
| 模型训练   | PyTorch            |
| 人物重识别 | FastReID           |
| 模型推理   | ONNX Runtime       |

Scenara Console 由核心平台统一建设。数据平台和模型平台通过 API、路由模块和产品目录接入统一 Console，不得自行选择第二套前端框架并复制登录、权限、导航和设计系统。

技术栈变更必须提交 ADR，至少说明迁移成本、兼容影响、运维影响、替代方案和退出计划。框架升级不得绕过锁文件、自动化测试和生产构建验证。

------

## 42.1 前端设计系统与平台主题规范

本章是所有用户界面的强制规范，适用于统一 Console、专业平台路由模块、登录页、管理后台和嵌入式工作台。它解决两个目标：

```text
一套景枢产品体验
不同平台可识别的工作区主题
```

平台主题只能改变受控的强调色和少量领域装饰，不得复制一套登录、导航、权限、布局或组件库。当前 `scenara` Console 已有的石墨侧栏、浅色内容区、青绿色主操作色和珊瑚/绿色/琥珀语义色是第一阶段基线；后续实现应优先抽取为令牌，而不是整体改版。

### 42.1.1 品牌与视觉边界

必须遵守：

```text
产品名称：Scenara 景枢
产品类别：视觉 AI 中枢平台
品牌口号：连接视觉 · 理解世界（仅用于登录、欢迎和品牌展示区域）
```

品牌标志只能使用 `docs/brand/assets/` 中的标准资产。全彩标志的紫色、电光蓝、青色渐变只允许出现在标志、应用图标和明确的品牌展示区域；不得把该渐变作为页面背景、按钮背景、大面积导航或数据图表配色。禁止自行重绘、变色、旋转、拉伸、描边或添加阴影。

Logo 几何必须遵守 [ADR 0004](docs/adr/0004-logo-geometry-alignment.md)：标准母体使用 `64 x 64` 视图框和 `x = 32` 中轴，中心四向星位于 `(32, 32)`；左侧/上方曲面带上移 3 个单位，右侧/下方曲面带由同一母路径旋转 180°后下移 3 个单位。所有字标、应用图标、favicon 和 Console 副本必须同步使用该几何，不得在业务代码中维护另一套手调路径。

界面默认采用浅色内容区和深色导航区。深色模式不是浏览器自动推断结果；只有在完整定义对比度、图表、图片、弹层和状态色并通过验收后，才可以通过 ADR 启用。未完成深色模式前必须声明 `color-scheme: light`，不得出现半套暗色界面。

### 42.1.2 共享设计令牌

所有 Vue 页面必须通过共享 CSS 令牌或等价的 TypeScript 主题对象取色，禁止在业务组件中散落无法解释的颜色值。令牌至少包括：

```text
--color-brand-mark
--color-bg
--color-surface
--color-surface-muted
--color-text
--color-text-muted
--color-border
--color-border-strong
--color-accent
--color-accent-hover
--color-accent-soft
--color-success
--color-warning
--color-danger
--color-info
--color-sidebar
--font-sans
--font-mono
--space-1 ... --space-8
--radius-sm / --radius-md
--shadow-sm / --shadow-md
```

第一阶段默认值必须与当前 Core 实现兼容：

| 令牌 | 基线值 | 用途 |
| --- | --- | --- |
| `--color-bg` | `#F3F5F4` | 页面背景 |
| `--color-surface` | `#FFFFFF` | 面板、表格和表单背景 |
| `--color-text` | `#17211F` | 正文和标题 |
| `--color-text-muted` | `#64716D` | 辅助文字 |
| `--color-border` | `#D9DFDC` | 默认边界 |
| `--color-sidebar` | `#111A18` | 共享导航 |
| `--color-accent` | `#087682` | Core 主操作和焦点 |
| `--color-success` | `#27764C` | 成功、完成、健康 |
| `--color-warning` | `#A96B12` | 等待、暂停、风险 |
| `--color-danger` | `#B34132` | 失败、删除、阻断 |
| `--color-info` | `#2264A6` | 信息和链接 |

颜色命名按语义而不是色相命名。组件必须支持 hover、active、focus-visible、disabled、loading、error、empty 和 selected 状态。状态不得仅由颜色表达，必须同时提供文字、图标、形状或位置差异。

### 42.1.3 平台主题矩阵

平台使用同一套布局和组件，仅通过平台主题令牌标识工作区。主题色必须满足正文/背景对比度至少 4.5:1，大字号至少 3:1；浅色背景上的主题色用于边框、图标或文字时必须使用通过对比度测试的深色变体。

| 平台 | 产品定位 | 视觉性格 | `data-platform` | 强调色 | 推荐使用位置 | 禁止事项 |
| --- | --- | --- | --- | --- | --- | --- |
| 核心平台 | 生产运行、视觉分析、特征和检索 | 稳定、克制、实时 | `core` | `#087682` 青绿 | 主按钮、当前导航、焦点、运行状态 | 不得使用高饱和渐变替代状态语义 |
| 数据平台 | 数据资产、数据集、标注和质量 | 清晰、可追溯、信息密集 | `data` | `#2F6B8A` 数据蓝 | 数据集导航、版本标签、质量指标 | 不得用颜色代替版本、血缘或质量文字 |
| 模型平台 | 训练、评估、注册和制品 | 实验性、可比较、可复现 | `model` | `#6256A8` 受控靛紫 | 实验、评估、模型版本和指标图表 | 不得大面积使用紫色背景或紫蓝渐变 |
| 平台契约 | 能力、实体、接口和事件契约 | 精确、文档化、开发者友好 | `contracts` | `#53636A` 中性蓝灰 | 契约状态、代码和版本信息 | 不得引入业务平台专属装饰 |

每个平台必须同时定义完整的强调色组，禁止在组件内临时计算颜色：

| 平台 | `accent` | `accent-hover` | `accent-soft` |
| --- | --- | --- | --- |
| Core | `#087682` | `#065E67` | `#E4F1F1` |
| Data | `#2F6B8A` | `#24566F` | `#E8F1F6` |
| Model | `#6256A8` | `#4E438A` | `#F0EDF8` |
| Contracts | `#53636A` | `#3F4D53` | `#EEF1F2` |

主题实现必须通过根节点属性或等价机制切换，例如：

```html
<div id="app" data-platform="core">
```

主题切换只允许覆盖 `--color-accent`、`--color-accent-hover`、`--color-accent-soft` 及必要的图表序列色；不得覆盖全局字体、间距、圆角、导航结构和语义成功/警告/危险色。一个页面只能有一个主平台主题；跨平台聚合页面使用 Core 主题，并在卡片或标签中标明数据来源平台。

### 42.1.4 布局、排版和组件规则

统一 Console 必须提供：

```text
固定或可折叠侧栏
顶部上下文栏（平台、页面、项目或租户上下文）
可跳过到主内容的键盘入口
页面标题、说明和主要操作区域
一致的面板、表格、表单、分页、弹层和通知组件
```

布局基线：

| 项目 | 规范 |
| --- | --- |
| 内容最大宽度 | `1420px`，宽屏居中；数据表允许横向滚动 |
| 侧栏 | 桌面约 `232px`，移动端改为抽屉，不得压缩主内容 |
| 顶栏 | 约 `58px`，始终显示当前平台和页面上下文 |
| 间距 | 采用 4px 基数，常用 8/12/16/24/32px |
| 圆角 | 控件 `4-6px`，面板不超过 `8px`；禁止胶囊化整页布局 |
| 阴影 | 仅用于弹层、悬浮和明确层级，不得用阴影替代边界 |
| 正文 | 中文优先使用系统无衬线字体；代码、ID、哈希和指标使用等宽字体 |
| 动效 | 默认 120-200ms；必须尊重 `prefers-reduced-motion` |

工作台排版使用固定层级，不得按视口宽度连续缩放字号：

| 层级 | 建议字号 | 使用范围 |
| --- | --- | --- |
| 辅助信息 | `12px` | 表格补充信息、时间、状态说明 |
| 控件与表格正文 | `13-14px` | 按钮、输入、导航、表格 |
| 面板标题 | `14-16px` | 面板、弹层和分组标题 |
| 页面标题 | `20-24px` | 页面主标题 |
| 品牌展示 | `28-58px` | 仅登录、欢迎或正式品牌展示区域 |

图表默认序列色为 `#087682`、`#6256A8`、`#2F6B8A`、`#9A527F`、`#7A6541`、`#5F6D69`。同一图表必须保持序列映射稳定，并同时使用线型、点型、标签或纹理区分；成功、警告和危险语义色不得被普通数据序列占用后造成含义冲突。

按钮、图标、表单、表格和面板必须遵循以下规则：

```text
主要动作每个视图最多一个视觉主按钮
工具操作优先使用 Lucide 图标，并提供 title 或 aria-label
删除、撤回、发布、准入等高风险操作必须二次确认并说明影响
表格表头、分页、筛选、排序、选择和批量操作必须可键盘操作
加载使用稳定尺寸的骨架或进度状态，禁止布局跳动
空状态必须说明原因和下一步动作，错误状态必须说明重试或修复路径
```

数据平台页面优先使用表格、树、版本时间线、质量摘要和血缘关系视图；模型平台页面优先使用实验对比、指标卡、评估曲线和制品状态视图；核心平台页面优先使用运行队列、结果预览、轨迹/检索和实时状态视图。领域差异体现在信息结构，不通过另起一套视觉组件实现。

### 42.1.5 响应式和无障碍门禁

至少验证 `1440x900`、`1280x800`、`768x1024` 和 `390x844` 四种视口。移动端必须满足：导航可打开和关闭、表格可横向滚动、主要操作不被遮挡、文本不溢出、弹层不超出视口、触控目标不小于 `44x44px`。不得通过隐藏关键字段或仅保留颜色来“适配”移动端。

新建和重构界面必须满足 WCAG 2.2 AA 的可操作基线；存量界面至少不得低于 WCAG 2.1 AA，并应纳入升级计划：

```text
正文与背景对比度至少 4.5:1，大字号至少 3:1
所有交互元素有可见 focus-visible 状态
完整键盘路径，不依赖鼠标悬停才能完成操作
表单控件有 label，图标按钮有 aria-label 或 title
图片、图表和状态提供可理解的替代文本
错误、成功、进度和连接状态同时提供文本或辅助技术可读信息
```

### 42.1.6 前端实现与验收证据

每个新增平台路由或主题必须同时提交：

```text
主题令牌定义及 data-platform 绑定
桌面和移动端截图或 Playwright 截图
键盘导航和 focus-visible 验证
颜色对比度检查结果
加载、空、错误、禁用和权限拒绝状态证据
组件/页面测试与构建结果
```

发布门禁必须检查：

```text
不存在未登记的主题色和品牌资产
没有复制登录、导航、IAM 或设计系统
四种视口无溢出和关键操作遮挡
主题切换不改变语义状态色和跨平台权限含义
页面标题、平台标识和错误状态可被键盘及辅助技术读取
```

无法满足本章要求时，只能标记为开发版或内部验证版；必须通过 ADR 记录例外、影响范围、责任人、复审日期和退出条件。

------

# 43. 第三方框架规范

第三方框架必须通过适配层接入。

例如：

```text
FastReID
 ↓
ReID Adapter

ONNX Runtime
 ↓
Inference Provider

S3-compatible Object Storage
 ↓
Object Storage Provider

pgvector
 ↓
Feature Store Provider
```

业务层不得直接绑定具体第三方实现。

------

# 44. 当前已有基础

## 44.1 景枢核心平台

目前已经具备基础的：

```text
项目管理
身份权限
媒体管理
运行管理
流程管理
算子管理
结果管理
模型管理
模型准入
模型发布
特征相关能力
反馈
难例
接口基础
```

核心平台已经形成视觉 AI 运行平台基础。

核心平台同时承载统一 Console、API、SDK、IAM、授权、审计和产品目录。该定位属于共享控制面，不代表核心平台拥有数据平台或模型平台的业务事实。

------

## 44.2 景枢模型平台

现有 `vision-model-lab` 已经具备模型训练平台基础。

包括：

```text
数据集管理基础
实验管理
训练
评估
模型导出
模型注册
模型制品
模型发布
模型回滚
模型审核
对象存储
```

并具备多个视觉模型训练方向的基础。

现在统一迁移为：

```text
scenara-model
```

后续按照本规范进行重构和扩展。

------

## 44.3 景枢数据平台

目前核心平台已经存在一定的数据集治理能力。

这些能力不再继续向核心平台扩展。

后续逐步迁移和重构至：

```text
scenara-data
```

核心平台仅保留与生产运行直接相关的数据引用和结果能力。

现有 Dataset、Dataset Version、Annotation Task 等实现是迁移来源，不是长期所有权依据。拆分必须通过契约、数据迁移、API 代理和验证切换完成，禁止只移动目录或复制数据库表。

------

## 44.4 景枢平台契约

新建：

```text
scenara-contracts
```

作为所有项目共同依赖的：

```text
公共数据结构
公共接口
公共事件
公共能力
公共实体
公共状态
公共错误码
```

唯一来源。

------

# 45. 当前最重要的开发任务

按照以下顺序执行。每项任务必须明确责任仓库、参与仓库、输入、输出、验收证据和未完成风险。

## 第一任务：建立平台契约

责任仓库：`scenara-contracts`。

参与仓库：全部项目。

完成：

```text
scenara-contracts
```

建立：

```text
能力词典
实体词典
数据结构
API
事件
错误码
状态
对象引用
版本规范
```

交付物至少包括：版本化 Schema、有效示例、变更记录、兼容性检查、生成类型或 SDK、发布摘要和消费方测试夹具。

------

## 第二任务：独立建设数据平台

责任仓库：`scenara-data`。

迁移提供方：`scenara`。

完成：

```text
scenara-data
```

首先实现：

```text
Dataset
Dataset Version
Sample
Annotation
Manifest
```

然后实现：

```text
Data Quality
Data Lineage
Hard Sample
Dataset Builder
```

创建独立仓库前必须完成：

```text
领域模型和责任人确定
HardSampleManifest 输入契约稳定
DatasetVersionReference 输出契约稳定
独立数据库和对象存储命名空间设计
IAM 与权限透传方案
审计事件回传方案
历史 Dataset 数据迁移和回滚方案
容量、备份、恢复和安全责任确定
```

核心平台保留稳定公共 API 和 Console 页面，通过 Data Client 或 API Gateway 访问数据平台。切换完成后，核心平台本地数据集表进入只读和退役流程。

------

## 第三任务：整理模型平台

责任仓库：`scenara-model`。

完成：

```text
scenara-model
```

重点：

```text
训练任务
实验
数据集版本引用
模型版本
模型评估
模型制品
模型注册
模型发布
```

模型平台只生成和发布不可变模型制品，不得直接修改核心平台运行时绑定或生产部署状态。

------

## 第四任务：接入 FastReID

责任仓库：`scenara-model`。

消费仓库：`scenara`。

完成：

```text
ReID Adapter
```

实现：

```text
Dataset Version
 ↓
Training Job
 ↓
FastReID
 ↓
Evaluation
 ↓
Model Package
```

交付物必须包括训练配置、代码版本、运行环境、数据集版本引用、Rank-1/5/10、mAP、模型卡、输入输出定义、制品摘要和许可信息。

------

## 第五任务：核心平台接入模型制品

责任仓库：`scenara`。

完成：

```text
Model Package
 ↓
Model Admission
 ↓
Model Deployment
 ↓
Model Runtime
```

核心平台必须完成制品引用解析、下载或挂载、摘要校验、Adapter 安装、健康检查、准入状态转换、激活、回滚和 Run 级绑定冻结。仅保存清单或切换 `runtime_model_id` 不视为完成模型加载。

------

## 第六任务：完成人物重识别生产闭环

责任仓库：`scenara`。

模型提供方：`scenara-model`。

完成：

```text
Person Detection
 ↓
Person Re-ID
 ↓
Feature
 ↓
Feature Store
 ↓
Person Search
```

图片检索响应必须一次性关联 Person、Track、Media、Timestamp、Similarity 和 Model Version，不得要求客户端自行拼接多个无契约关联的接口才能完成标准流程。

------

## 第七任务：完成端到端测试

责任：四个仓库共同负责，`scenara` 维护跨仓库验收编排，其他仓库提供可复现测试环境和固定输入。

必须验证：

```text
数据集
 ↓
训练
 ↓
评估
 ↓
模型
 ↓
部署
 ↓
推理
 ↓
特征
 ↓
检索
```

端到端测试必须使用已发布契约、独立服务进程和隔离的数据存储。单仓库内存适配器测试不能替代跨仓库端到端证据。

------

# 46. 第一阶段项目关系

最终必须达到：

```text
                    scenara-contracts
                           │
             ┌─────────────┼─────────────┐
             │             │             │
             ▼             ▼             ▼
        scenara-data  scenara-model   scenara
             │             │             │
             │             │             │
          Dataset       Training      Runtime
          Version       Evaluation    Inference
             │             │             │
             └─────────────┼─────────────┘
                           │
                         Feature
                           │
                         Search
```

------

# 47. 第一阶段验收标准

必须完整实现：

| 序号 | 验收动作 | 责任仓库 | 最低证据 |
| --- | --- | --- | --- |
| ① | 创建数据集 | `scenara-data` | API 测试、审计记录、持久化记录 |
| ② | 创建并发布不可变数据集版本 | `scenara-data` | Manifest、摘要、状态转换测试 |
| ③ | 数据集版本提供给模型平台 | Data + Contracts + Model | `DatasetVersionReference` 消费方兼容测试 |
| ④ | 创建人物重识别训练任务 | `scenara-model` | Training Job API 和任务记录 |
| ⑤ | FastReID 执行训练 | `scenara-model` | 可复现训练日志、配置和检查点 |
| ⑥ | 执行模型评估 | `scenara-model` | Rank-1/5/10、mAP 与固定评估集 |
| ⑦ | 生成模型制品 | `scenara-model` | Model Package、SHA-256、模型卡 |
| ⑧ | 注册并发布模型 | `scenara-model` | Model Version 和发布事件 |
| ⑨ | 模型准入 | `scenara` | 准入 API、证据校验、审计记录 |
| ⑩ | 模型激活 | `scenara` | 状态机、唯一能力绑定、部署事件 |
| ⑪ | 核心平台加载模型 | `scenara` | 制品解析、安装、摘要校验和健康检查 |
| ⑫ | 执行人物检测 | `scenara` | 固定媒体推理结果和模型来源 |
| ⑬ | 执行人物重识别 | `scenara` | ReID 推理结果和版本来源 |
| ⑭ | 生成人物特征 | `scenara` | Feature Space、维度、模型版本和来源 |
| ⑮ | 写入特征库 | `scenara` | FeatureStore 集成测试和租户隔离 |
| ⑯ | 使用图片进行人物检索 | `scenara` | 图片查询 API 和固定查询集 |
| ⑰ | 返回人物 | `scenara` | Person 标识和契约字段 |
| ⑱ | 返回轨迹 | `scenara` | Track 标识和媒体内时间范围 |
| ⑲ | 返回媒体 | `scenara` | Media 引用和访问权限验证 |
| ⑳ | 返回时间、相似度和模型版本 | `scenara` | UTC 时间、Similarity、Model Version |

全部通过后，第一阶段闭环完成。

单个仓库只对其责任项负责。平台级“第一阶段完成”由全部责任项和跨仓库端到端测试共同判定，任何仓库不得因自身责任项通过而宣称完整闭环完成。

------

# 48. 第二阶段开发

完成：

```text
人脸检测
人脸特征
人体姿态
人体属性
步态
行为
目标跟踪
跨摄像机人物关联
```

统一接入：

```text
Scenara Model
        ↓
Model Package
        ↓
Scenara Core
```

------

# 49. 第三阶段开发

建设统一视觉检索：

```text
Image Search
Person Search
Face Search
Video Search
Track Search
Text Search
Semantic Search
Temporal Search
Hybrid Search
```

------

# 50. 第四阶段开发

建设多模态能力：

```text
Image Embedding
Video Embedding
Text Embedding
Multimodal Embedding
```

实现：

```text
图搜图
图搜视频
文搜图
文搜视频
人物搜索
事件搜索
```

------

# 51. 第五阶段开发

建设：

```text
自动数据分析
自动难例发现
自动数据集构建
自动模型评估
自动模型选择
自动训练
模型效果分析
```

------

# 52. 长期模型能力体系

最终支持：

```text
人物检测
人物重识别
人脸检测
人脸识别
人体姿态
人体属性
步态识别
行为识别
目标检测
目标跟踪
OCR
场景理解
视觉语言模型
多模态模型
```

------

# 53. 长期数据体系

最终形成：

```text
Media
 ↓
Sample
 ↓
Annotation
 ↓
Dataset
 ↓
Dataset Version
 ↓
Feature
 ↓
Person
 ↓
Track
 ↓
Feedback
 ↓
Hard Sample
```

------

# 54. 长期模型体系

最终形成：

```text
Dataset Version
 ↓
Experiment
 ↓
Training Job
 ↓
Evaluation
 ↓
Model
 ↓
Model Version
 ↓
Model Package
 ↓
Model Registry
 ↓
Deployment
```

------

# 55. 长期智能体系

最终形成：

```text
视觉数据
 ↓
视觉模型
 ↓
视觉特征
 ↓
视觉知识
 ↓
视觉检索
 ↓
视觉分析
 ↓
智能理解
```

------

# 56. 长期数据飞轮

景枢必须持续形成：

```text
数据
 ↓
模型
 ↓
生产
 ↓
分析
 ↓
检索
 ↓
反馈
 ↓
难例
 ↓
数据
 ↓
训练
 ↓
新模型
```

该闭环作为长期核心架构。

------

# 57. 代码开发规范

代码必须：

```text
模块化
可测试
可扩展
可替换
可追踪
```

业务代码不得直接耦合：

```text
数据库实现
对象存储实现
向量数据库实现
具体训练框架
具体推理框架
```

必须使用抽象层。

抽象层必须以领域端口、Provider、Repository、Client 或 Adapter 表达明确责任，不得把所有依赖收敛到无类型字典或万能接口。接口实现必须可以通过契约测试替换。

跨仓库 Client 必须显式配置：基础地址、认证方式、超时、重试、幂等、熔断、追踪和错误映射。禁止在领域服务中直接调用未封装的 HTTP 客户端。

新增公共 API、事件、数据库迁移或跨仓库契约时，必须同时提交对应测试和文档。纯占位返回、始终成功的探针和没有失败路径的模拟实现不得标记为完成。

------

# 58. 项目目录规范

新建项目推荐采用：

```text
src/
tests/
docs/
scripts/
configs/
deploy/
```

已有项目可以保留稳定的顶层 Python 包或框架目录，例如 `scenara/`。不要求仅为目录形式统一迁移到 `src/`，但必须满足：

```text
领域、平台、基础设施和适配层边界清晰
测试能够验证依赖方向
迁移兼容目录有明确退出条件
配置、部署和文档可发现
业务代码不依赖工作目录偶然结构
```

具体模块按照领域能力划分。

禁止长期形成：

```text
utils/
common/
misc/
helpers/
```

作为无边界代码堆积目录。

如确需使用上述名称，必须限定职责、所有者和允许依赖方向，并通过架构测试防止继续堆积。

------

# 59. 文档规范

所有项目必须同时维护：

```text
README.md
架构说明
开发规范
API 文档
数据模型文档
部署文档
测试文档
变更记录
责任人或责任团队
当前成熟度和未完成门禁
```

项目内部文档使用中文为主。

技术名称、代码标识、接口、类名、字段名使用英文。

文档中的“完成、可用、生产就绪、已支持”必须有可复现证据链接。规划能力、Seed 能力、Fallback 和 Placeholder 必须明确标识，不得使用面向生产的表述。

架构决策、长期例外、数据迁移和不兼容变更必须维护 ADR。API 文档和生成 SDK 必须通过 CI 检查与实现零漂移。

------

# 60. AI 开发规范

所有需要 AI Coding Agent 开发的任务必须优先参考：

```text
scenara-contracts
    ↓
景枢能力词典
    ↓
景枢实体词典
    ↓
景枢接口契约
    ↓
目标项目开发规范
    ↓
现有代码
```

AI 不得自行重新定义：

```text
实体名称
能力名称
接口名称
状态
错误码
跨项目数据结构
```

如发现现有代码与契约不一致：

> 以正式契约为准，并通过迁移修正代码。

------

# 61. 开发红线

以下行为禁止：

```text
禁止跨项目直接访问数据库

禁止跨项目直接引用源码

禁止覆盖已发布数据集版本

禁止覆盖已发布模型版本

禁止未经模型准入进入生产

禁止未定义契约的跨项目接口

禁止跨项目传递本地文件路径

禁止在核心平台继续建设完整训练系统

禁止在核心平台继续建设完整数据集治理系统

禁止模型平台实现生产运行逻辑

禁止数据平台实现模型训练逻辑

禁止建立第二套身份认证体系

禁止业务代码直接绑定具体基础设施

禁止绕过平台契约新增跨项目数据结构
```

------

# 62. 版本规范

统一：

```text
主版本.次版本.修订版本
```

例如：

```text
1.0.0
1.1.0
1.1.1
2.0.0
```

规则：

```text
修订版本
→ 问题修复

次版本
→ 向后兼容的新功能

主版本
→ 不兼容变更
```

------

# 63. 完成标准

任何跨项目功能必须按责任仓库满足以下完成定义。不适用项可以标记为 `N/A`，但必须写明理由和批准人，不得直接删除：

```text
□ 能力已定义
□ 能力 ID 已定义
□ 实体已定义
□ 契约已定义
□ API 已定义
□ 数据结构已定义
□ 权限已定义
□ 错误码已定义
□ 事件已定义
□ 对象引用已定义
□ 单元测试完成
□ 接口测试完成
□ 集成测试完成
□ 端到端测试完成
□ 日志完成
□ 审计完成
□ 文档完成
□ 兼容性检查完成
□ 迁移和回滚方案完成
□ 责任仓库和运行负责人明确
□ 生产证据或明确门禁状态已记录
```

完成状态分为：

| 状态 | 定义 |
| --- | --- |
| planned | 已进入路线图，尚无可使用实现 |
| seed | 有接口或最小实现，不具备完整场景闭环 |
| implemented | 代码和单元测试完成，尚未完成目标环境资格验证 |
| qualified | 在规定环境、数据和容量下通过可复现验证 |
| production_ready | 契约、运维、安全、恢复、许可和发布证据全部通过 |

禁止把 `seed`、`implemented` 或开发 fallback 表述为 `production_ready`。

------

# 64. 最终平台形态

景枢长期形成：

```text
                         景枢 Scenara
                              │
                              ▼
                    ┌──────────────────┐
                    │ 景枢平台契约      │
                    │ Scenara Contracts │
                    └────────┬─────────┘
                             │
             ┌───────────────┼───────────────┐
             │               │               │
             ▼               ▼               ▼
       景枢数据平台      景枢模型平台      景枢核心平台
      Scenara Data     Scenara Model     Scenara Core
             │               │               │
          数据资产          模型资产          智能运行
             │               │               │
             └───────────────┼───────────────┘
                             │
                             ▼
                         特征体系
                             │
                             ▼
                         检索体系
                             │
                             ▼
                         分析体系
                             │
                             ▼
                         智能体系
```

最终形成：

```text
数据
 ↓
模型
 ↓
运行
 ↓
特征
 ↓
检索
 ↓
分析
 ↓
反馈
 ↓
数据
 ↓
模型
```

的完整视觉人工智能平台。

------

# 65. 四个项目最终定位

| 项目                | 核心定位                             | 不负责                 |
| ------------------- | ------------------------------------ | ---------------------- |
| `scenara`           | 共享控制面、生产运行、视觉分析、特征、检索 | 模型训练、完整数据治理 |
| `scenara-data`      | 数据资产、数据集、标注、数据质量     | 模型训练、生产推理     |
| `scenara-model`     | 训练、评估、模型、模型制品           | 数据资产管理、生产运行 |
| `scenara-contracts` | 统一能力、实体、接口、事件和数据契约 | 业务逻辑               |

------

# 66. 核心原则

景枢所有后续开发统一遵守：

```text
一个平台
四个项目
一套契约
一套术语
一套基础设施
一套数据标准
一套模型标准
一套版本规范
```

系统之间：

```text
通过契约连接
通过 API 通信
通过事件协同
通过对象引用共享数据
通过统一身份体系进行权限控制
```

最终实现：

> **景枢数据平台负责数据，景枢模型平台负责模型，景枢核心平台负责共享控制面和生产智能能力，景枢平台契约负责统一语言和边界。**

这份文档应作为四个仓库的**上位开发规范**。任何项目级开发规范、AI Coding 规则、接口实现和数据结构设计均不得与本规范冲突。

------

# 67. 共享控制面接入规范

Scenara Core 是统一控制面和生产运行入口。专业平台接入必须遵循：

```text
统一 Console 导航
统一 API 入口或明确的服务间 API
统一 SDK 发布
统一 Organization / Project 上下文
统一身份签发和权限 ID
统一审计查询
统一请求与追踪上下文
```

数据平台和模型平台可以拥有独立服务和数据库，但不得复制用户密码、Membership、Role、API Key 和产品授权事实。服务间认证应使用核心平台签发或信任的短期凭据、服务账号或标准身份令牌。

Core 代理专业平台 API 时必须透传：

```text
tenant_id / organization_id
project_id
principal_id
permission scopes
request_id
traceparent / trace_id
idempotency key
```

专业平台必须独立执行授权检查，不得仅依赖“请求来自 Core”作为授权依据。

------

# 68. 契约发布与兼容规范

跨仓库契约发布流程：

```text
提出契约变更
 ↓
更新能力和实体词典
 ↓
更新 Schema / OpenAPI / Event
 ↓
补充有效与无效示例
 ↓
运行生产方验证
 ↓
运行消费方兼容测试
 ↓
生成发布摘要和制品
 ↓
发布版本
 ↓
各消费仓库升级锁定版本
```

兼容性规则：

| 变更 | 版本要求 |
| --- | --- |
| 文档修正且不改变机器可读契约 | 修订版本 |
| 新增可选字段、事件或向后兼容 API | 次版本 |
| 删除字段、增加必填字段、收窄类型或改变含义 | 主版本 |

契约发布必须提供摘要锁定、确定性构建和上一版本兼容检查。消费方不得依赖未发布分支、仓库源码或内部 Pydantic/ORM 模型。

弃用必须先标记，再经过至少一个约定兼容周期。弃用公告必须包含替代方案、影响范围、迁移步骤和停止支持版本。

------

# 69. 数据平台拆分与迁移规范

Core 中现有的数据集和标注能力拆分到 `scenara-data` 时，按以下顺序执行：

```text
定义目标领域模型和契约
 ↓
在 Core 建立 DataPlatformClient 端口
 ↓
使用现有本地实现作为兼容 Adapter
 ↓
建设独立 Data 服务、数据库和对象存储边界
 ↓
全量迁移并校验数量、摘要和不可变版本
 ↓
进行影子读和结果比对
 ↓
短时冻结或增量同步
 ↓
切换 Core 到远程 Data Client
 ↓
旧表只读观察
 ↓
完成回滚窗口后退役旧写路径
```

禁止长期双写。确需零停机迁移时，增量同步必须使用 Outbox、CDC 或版本化事件，并有去重、顺序和重放策略。

现有业务 ID 在迁移期间保持不变。不得把拆仓与 ID 重编码、状态重命名、API 路径变更同时进行。

已发布数据集版本迁移后必须保持：

```text
版本标识不变
Manifest 摘要不变或有可验证迁移映射
样本内容不可变
来源血缘可追踪
权限不扩大
审计记录可查询
```

Core 的 Media、Run、Result、Person、Track、Feature 和 Feedback 保持 Core 所有。Data 通过对象引用、样本副本或授权 API 使用来源内容，不得读取 Core 数据库表。

------

# 70. 测试与验证矩阵

每个项目至少维护：

| 测试类型 | 验证目标 | 是否可由 Mock 替代 |
| --- | --- | --- |
| 单元测试 | 领域规则、状态机、校验和错误路径 | 可以 |
| 契约测试 | Schema、示例、生产方与消费方兼容 | 不可以 |
| API 测试 | 权限、幂等、分页、错误码、版本 | 部分可以 |
| 适配器测试 | PostgreSQL、Redis、对象存储、推理/训练 Provider | 不可以 |
| 集成测试 | 独立服务和真实基础设施协作 | 不可以 |
| 端到端测试 | 用户场景和跨仓库闭环 | 不可以 |
| 容量测试 | 吞吐、延迟、显存、背压和恢复 | 不可以 |
| 恢复测试 | 备份、恢复、重放和灾难切换 | 不可以 |

测试必须覆盖成功、失败、超时、重试、重复请求、权限拒绝、部分依赖不可用和恢复场景。跳过的测试必须输出原因；发布门禁不得把跳过视为通过。

固定评估集、测试制品和运行环境必须版本化并记录摘要。仅有测试数量或覆盖率不能替代业务验收证据。

------

# 71. 安全与隐私规范

所有项目必须执行最小权限、租户隔离、输入验证、敏感信息脱敏和依赖安全检查。

以下数据按敏感数据处理：

```text
人脸和人体特征
身份关联结果
轨迹和摄像机位置
原始媒体
访问凭据
模型与数据许可证明
审计记录中的个人信息
```

敏感数据必须定义：用途、访问权限、存储位置、保留周期、删除流程、导出限制和审计要求。Feature、Person、Track 和 Hard Sample 的删除必须可追踪，并处理派生索引和对象存储副本。

外部 URL、媒体、压缩包、模型包和文档输入必须防护 SSRF、路径穿越、压缩炸弹、恶意文件和资源耗尽。密钥不得进入代码、镜像、日志、测试夹具或发布证据。

------

# 72. 发布与运行证据规范

生产发布至少需要：

```text
版本和变更记录
契约兼容报告
单元、接口、集成和端到端结果
安全扫描和依赖清单
数据库迁移与回滚验证
对象存储和备份恢复证据
目标环境容量报告
模型和数据许可记录
已知限制与未完成门禁
运行负责人和告警入口
```

发布证据必须绑定准确的软件版本、配置摘要、契约版本和执行时间。历史版本证据不得自动复用于新版本。

生产发布门禁必须失败关闭。缺少必需证据时可以发布开发版或内部验证版，但不得标记为 `production_ready`。

------

# 73. 变更、迁移与弃用规范

任何不兼容变更必须包含：

```text
影响分析
数据迁移脚本
回滚脚本或恢复方案
双版本兼容窗口
SDK 和 Console 适配
监控指标
停止旧版本的明确日期或版本
```

迁移脚本必须可重复执行或能检测已执行状态。禁止依赖人工修改生产数据库完成正式迁移。

公共 API、事件、能力 ID、错误码和持久化 ID 不得无公告删除。安全问题需要紧急移除时，必须记录安全例外、影响和补救措施。

------

# 74. 规范符合性检查

每个仓库应提供自动化符合性门禁，至少检查：

```text
契约与生成代码零漂移
禁止跨仓库源码导入
禁止跨仓库数据库访问
能力 ID 和错误码已登记
公开时间字段符合 RFC 3339
敏感信息和密钥扫描
架构依赖方向
迁移文件顺序和不可变性
版本一致性
文档中的成熟度声明有证据
前端主题令牌、品牌资产和平台标识符合设计系统
规定视口无溢出、遮挡和不可达关键操作
键盘导航、焦点状态和颜色对比度符合无障碍门禁
```

规范检查失败必须阻断正式发布。无法自动化的条目进入发布清单，由明确负责人签署结果和证据位置。
